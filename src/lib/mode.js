/*
 * У приложения два лица, и это главное решение всей публикации.
 *
 * npm run dev — мастерская: IndexedDB, привязанная папка, вся правка.
 * Собранный сайт (то, что уезжает на GitHub Pages) — витрина: только чтение
 * готовых файлов, лежащих рядом с сайтом в data/.
 *
 * Поэтому «менять контент могу только я» получается само собой: в том, что
 * стоит в интернете, нет ни одной кнопки, которая пишет, и нет доступа к диску.
 * Содержимое меняется одним способом — правкой у себя и git push.
 */
export const VIEWER = import.meta.env.PROD
export const EDITOR = !VIEWER

// data/ лежит рядом с сайтом, поэтому путь считаем от базового адреса:
// на project-страницах GitHub сайт живёт не в корне домена.
export const dataUrl = (path) => `${import.meta.env.BASE_URL}data/${path}`
